package com.yiyao.object;

/**
 * 创建对象:
 *
 * 1.先声明 在创建
 * Car car; //声明
 * car = new Car(); //创建 < 在堆里创建空间，地址也指向栈里的 car
 *
 * 2.直接创建
 * Car car = new Car();
 *
 *
 *
 * 访问属性: 对象名.属性
 *
 * <p></p>
 * <b>类与对象的内存分配机制(重要!!!)</b>
 */
public class Object03 {
}
